class MobileNavbar {
    constructor(mobileMenu, menuButton, navLinks) {
        this.mobileMenu = document.querySelector(mobileMenu);
        this.menuButton = document.querySelector(menuButton);
        this.navLinks = document.querySelector(navLinks);
    }
}